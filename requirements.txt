Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
python-dotenv==1.2.2
requests==2.34.2
# Pinned explicitly (rather than left to Flask's floor) so the transitive
# dependency can't resolve to a version with known advisories.
Werkzeug==3.1.8

pytest==9.1.1
